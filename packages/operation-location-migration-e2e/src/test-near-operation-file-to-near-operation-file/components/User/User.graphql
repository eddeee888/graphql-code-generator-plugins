query User($id: ID!) {
  user(id: $id) {
    ...UserFragment
  }
}

mutation UpdateUser {
  updateUser(input: { id: "100", name: "Tom" }) {
    id
    name
  }
}

subscription UserChanges {
  userChanges(id: "100") {
    ...UserFragment
  }
}
