extend type Subscription {
  profileChanges: StudentProfile!
}

extend type Student {
  profile: StudentProfile
}

type StudentProfile {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  phoneNumber: String
}
