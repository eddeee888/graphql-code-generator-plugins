import type { UserResolvers } from './../../types.generated';
export const User: Pick<
  UserResolvers,
  'id' | 'username' | '__isTypeOf' | '__resolveReference'
> = {
  /* Implement User resolver logic here */
};
