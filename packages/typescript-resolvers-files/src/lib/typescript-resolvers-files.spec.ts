import { typescriptResolversFiles } from './typescript-resolvers-files';

describe('typescriptResolversFiles', () => {
  it('should work', () => {
    expect(typescriptResolversFiles()).toEqual('typescript-resolvers-files');
  });
});
